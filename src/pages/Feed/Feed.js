import classNames from "classnames/bind"
import styles from './Fedd.module.scss'

const cx = classNames.bind(styles)


function Feed() {
  return (
      <div className={cx('container')}>
        
      </div>
  )
}

export default Feed